{
  "type": "mysql",
  "host": "localhost",
  "port": 3306,
  "username": "root",
  "password": "password",
  "database": "cuw_dev",
  "entities": ["dist/src/**/*.entity{.js, .ts}"],
  "migrations": ["dist/migrations/*{.js, .ts}"],
  "synchronize": true,
  "logging": true,
  "cli": {
    "migrationsDir": "migrations"
  }
}
